# 멀티 호라이즌 모멘텀 외부 검증 결과

## 판정

`multi_momentum_scale0.75_votes3_stop8_trail16_long_only` 기준선은 외부 검증에서 `REJECTED`다. 이 결과는 기존 개발 구간의 양수 성과를 부정하는 것이 아니라, 2024년 이후 신규 구간에 안정적으로 일반화되지 않았음을 의미한다.

실행은 다음 봉인 프로토콜로 재현했다.

```bash
node scripts/volume-flow-feature-discovery.mjs \
  --db build/runtime-test/bybit-trader-full-history.sqlite \
  --windows config/multi-horizon-momentum-validation-windows-v1.json \
  --out build/multi-horizon-momentum-validation-v1/base \
  --profile multi-horizon-momentum \
  --candidateId multi_momentum_scale0.75_votes3_stop8_trail16_long_only \
  --targetCdrPct 0.2 --quiet true
```

## 기본 비용 결과

| 항목 | 결과 | 승인 기준 |
|---|---:|---:|
| 외부 구간 | 10개 | 8개 이상 |
| 양수 구간 | 3/10 (`30%`) | 75% 이상 |
| 중앙 일복리 | `-0.02455%` | 0% 초과 |
| 평균 일복리 | `-0.02084%` | 0% 초과 |
| 최악 일복리 | `-0.07097%` | 0% 초과 |
| 총 거래 | 136건 | 200건 이상 |
| 최대 낙폭 | `6.25638%` | 30% 이하 |

구간별 양수는 `V01`, `V08`, `V09`뿐이다. `V02`의 손익은 `-6.25638%`, `V10`은 `-4.67380%`로 최근 구간에서도 방향이 안정적이지 않았다.

## 비용 스트레스

| 비용 배수 | 중앙 일복리 | 양수 구간 | 최대 낙폭 |
|---:|---:|---:|---:|
| 1.0x | `-0.02455%` | 3/10 | `6.25638%` |
| 1.5x | `-0.03451%` | 2/10 | `7.57014%` |
| 2.0x | `-0.03957%` | 1/10 | `9.07743%` |

손실의 주원인이 단순히 비용이라고 단정할 수는 없다. 기본 비용에서도 중앙값이 음수이고, `V02`, `V03`, `V05`, `V06`, `V07`, `V10`에서 profit factor가 1 미만이다. 비용은 이미 약한 우위를 추가로 훼손한다.

## 다음 실험의 제한

이 후보를 다시 같은 구간에서 조정하지 않는다. 다음 단계는 새 봉인 구간을 소비하지 않고 학습 구간에서만 다음 세 축을 각각 최대 24개 이하로 시험한다.

1. 신호 후 진입: 다음 M5 시가 기준선과 M1 연속성 확인
2. 청산: ATR trailing 갱신 지연과 최대 보유 시간
3. 레짐 필터: 닫힌 M15 방향 필터의 추가 효과

각 실험은 기본 기준선과 비용 1.5배·2배를 함께 기록한다. 외부 검증 실패를 본 뒤 위험 비율을 높이는 방식은 허용하지 않는다.
